extends ../layout

block content
    a.back-link(href="/") &larr; На главную
    h1= title
    table
        tr 
            th id
            th Имя
        each item in clients
            tr
                td= item.id
                td= item.label
